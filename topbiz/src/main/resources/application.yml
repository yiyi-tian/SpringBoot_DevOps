server:
  port: 8080

spring:
  application:
    name: topbiz
  redis:
    host: localhost
    port: 6379

shiro:
  sessionManager:
    sessionIdUrlRewritingEnabled: false
  redis:
    host: localhost
    port: 6379

feign:
  client:
    config:
      default:
        connectTimeout: 3000
        readTimeout: 10000