apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: devops
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
        - name: user-service
          image: docker.io/library/infra-user-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8081
          envFrom:
            - configMapRef:
                name: devops-app-config
            - secretRef:
                name: devops-app-secret
          env:
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:mysql://mysql:3306/devops_user?serverTimezone=Asia/Shanghai&characterEncoding=utf8"
          volumeMounts:
            - name: access-logs
              mountPath: /app/logs/access
          readinessProbe:
            tcpSocket:
              port: 8081
            initialDelaySeconds: 30
            periodSeconds: 10
      volumes:
        - name: access-logs
          persistentVolumeClaim:
            claimName: access-logs
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: devops
spec:
  type: ClusterIP
  selector:
    app: user-service
  ports:
    - port: 8081
      targetPort: 8081
